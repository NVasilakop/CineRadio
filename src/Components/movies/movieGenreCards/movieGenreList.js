import MovieGenre from './movieGenre.js';

function MoviesGenreList(props) {
    return (
        // <div>
        //     {props.moviesgenres.map(c => <MovieGenre key={c.id} movieName={c.name} />)}
        // </div>
        <div className="row">
            {this.ge}
        </div>
    );
}

export default MoviesGenreList;